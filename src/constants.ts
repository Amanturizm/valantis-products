export const API_URL = 'https://api.valantis.store:41000/';

export enum ActionType {
  FILTER = 'filter',
  GET_IDS = 'get_ids',
  GET_ITEMS = 'get_items',
  GET_FIELDS = 'get_fields',
}

export const PRODUCTS_LENGTH = 50;
